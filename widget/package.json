{
  "name": "marv-widget",
  "version": "2.0.0",
  "description": "MARV AI Damage Assessment Widget",
  "type": "module",
  "scripts": {
    "build": "esbuild marv-widget.js --bundle --outfile=dist/marv-widget.bundle.js --format=iife --minify",
    "dev": "esbuild marv-widget.js --bundle --outfile=dist/marv-widget.bundle.js --format=iife --watch",
    "build:dev": "esbuild marv-widget.js --bundle --outfile=dist/marv-widget.bundle.js --format=iife"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
